# Mullvad WireGuard configuration
# Get these from https://mullvad.net/en/account/wireguard-config
WIREGUARD_PRIVATE_KEY=
WIREGUARD_ADDRESSES=

# Mullvad server location (optional, defaults to Switzerland)
# See https://github.com/qdm12/gluetun-wiki/blob/main/setup/providers/mullvad.md
SERVER_COUNTRIES=Canada

# Tailscale auth key (generate at https://login.tailscale.com/admin/settings/keys)
# Use a reusable key so the container can reconnect after restarts
TS_AUTHKEY=tskey-

# Hostname for the exit node as it appears in your tailnet
TS_HOSTNAME=
